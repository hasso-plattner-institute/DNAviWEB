# This empty file makes Python treat this directory (database) as a package.
# Without this we can't import submodules or subpackages using the full dotted path,
# like "from database.config import engine".
