# This empty file makes Python treat this directory (schema) as a package.
# Without this we can't import submodules or subpackages using the full dotted path,
# like "from database.schema.base import Base".
